/* eslint-disable */
// Application config.
const AppConfig = {
  version: '0.1.2',
  appName: 'Books API app',

  // Various values for default states.
  api: {
    statusSuccess: 'SUCCESS',
    statusError: 'ERROR',
  },

  // Various values for default states.
  defaults: {
    foo: 'bar',
  },
};

module.exports = AppConfig;
